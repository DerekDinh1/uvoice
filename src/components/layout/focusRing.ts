// Shared focus-visible ring applied to interactive layout elements (nav
// links, theme toggle, buttons, the focusable main region) so keyboard focus
// stays clearly visible against the accent-colored active state instead of
// relying on the low-contrast default browser outline.
export const FOCUS_RING =
  'focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent focus-visible:ring-offset-2 focus-visible:ring-offset-bg';
